---
name: wf-code-generator
description: 代码生成。读取 spec 规格文件，按修改点指令生成代码，写入项目代码仓库。过程记录写入 CONTEXT/ISSUES/JOURNAL。
---

# 代码生成

按开发规格逐修改点生成代码，写入项目代码仓库。

> 在 workspace 目录下执行。

## 前置检查

当前目录存在 `CONTEXT.md`。不存在 → 报错终止。

## 执行流程

### Step 1：确认目标

确定要实现的规格文件：

- 用户指定 T-XXX → 直接使用
- 未指定 → 读 CONTEXT.md 规格索引，按以下规则选择：
  1. 排除已标记 ✅ 的任务
  2. 优先选无依赖或依赖已 ✅ 的任务
  3. 同优先级按 T-xxx 编号顺序

无可用任务 → 告知用户：**「所有任务已完成」**。

`output/specs/T-XXX.md` 存在。不存在 → 报错：**「未找到 specs/T-XXX.md」**，终止。

### Step 2：读取规格

读 `output/specs/T-XXX.md`，理解：任务概述、依赖关系、关键行为、实现方案、修改点清单。

检查依赖：从 spec 的「依赖」段提取，对照 CONTEXT.md 规格索引。有未完成的依赖 → 警告用户但继续执行。

读 `CONTEXT.md`，提取代码路径。代码路径为空 → 报错：**「CONTEXT.md 中未配置代码路径，无法生成代码」**。

### Step 3：生成代码

对每个修改点，**以实现任务为目标，不盲从 spec**：

1. **定位文件**：规格中的文件路径，`[NEW]` 标记的创建新文件（父目录不存在则自动创建）
2. **读取现有代码**：修改已有文件时，先读取理解上下文
3. **生成修改**：按规格指令和约束条件生成代码
4. **不超出规格范围**：只修改相关文件和位置，不做额外"优化"

**遇到以下情况时写入 ISSUES.md，不自行决定：**

- spec 与实际代码结构不符（文件不存在、类名错误等）
- 规格描述的实现方式无法落地或有明显缺陷
- 发现有更好的实现方案
- 关键行为描述与修改点指令矛盾

**生成完成后必须同步 spec：** 代码生成完毕，将 spec 的「修改点」更新为实际实现的文件、位置和内容，保证 spec 是代码的准确描述。

### Step 4：更新工作空间文件

**更新 CONTEXT.md：**

- 在 `规格` 索引中标注已完成的任务（`T-001 — ✅`）
- 在 `## 代码产出` 表格中标注（`T-001 | ✅ 已完成`）
- 如全部任务完成 → `阶段` 改为 `代码生成完成`，`下一步` 改为 `执行 wf-test-generator`

**写入 ISSUES.md（实现过程中发现的问题）：**

在 `## 实现阶段` 下按以下格式追加，编号按已有 Q-xxx 递增：

```
### Q-001 — [问题简述]

- **问题：** [具体描述]
- **AI 建议：** [建议方案]
- **影响：** [影响的文件或任务]
- **提出：** YYYY-MM-DD wf-code-generator

---

**人工决策：**
**状态：** 待决策
```

**写入 JOURNAL.md：**

在文件末尾追加当天日期标题（如已存在则复用），然后追加：

```
### HH:MM — wf-code-generator 完成 T-XXX

- 任务：[标题]
- 修改 X 个文件，新增/修改约 X 行
- 发现问题 X 项，写入 ISSUES.md
```

---

## 输出前自检

- [ ] 每个修改点已生成对应代码
- [ ] 代码遵循规格中的关键行为和约束条件
- [ ] 未修改规格列出的文件之外的代码
- [ ] 与 spec 有差异的地方已写入 ISSUES.md 并说明原因
